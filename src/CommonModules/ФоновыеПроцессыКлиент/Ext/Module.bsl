
&НаКлиенте
//
//
Процедура ВыполнитьКоманду(Команда, ЭтаФорма, Объект) Экспорт
	Если ЗначениеЗаполнено(Команда) Тогда		
		Попытка				
			Выполнить(Команда);
		Исключение
			ОбщегоНазначенияСервер.ВывестиСообщение(ОписаниеОшибки());
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры