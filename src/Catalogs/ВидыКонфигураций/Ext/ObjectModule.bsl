
//
//
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЗначениеЗаполнено(СокрЛП(ЭтотОбъект.Синоним)) Тогда
		ЭтотОбъект.Синоним = СокрЛП(ЭтотОбъект.Наименование);
	КонецЕсли;
	
	ЭтотОбъект.Редакция = СокрЛП(ЭтотОбъект.Редакция);
	
КонецПроцедуры
